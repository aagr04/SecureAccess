app.security.cookie.secure=true
app.security.cookie.same-site=None
# For production HTTPS. If frontend and backend are cross-site, keep SameSite=None with Secure=true.
# Replace the fallback with the real frontend domain or set APP_CORS_ALLOWED_ORIGINS in the platform.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:https://CAMBIAR-POR-DOMINIO-FRONTEND}
